import React from "react"

import Layout from "../components/layout"

const NotFound = () => (
  <div>
    <Layout>

      <h1 style={{
        padding: "20vh 0",
        textAlign: "center",
        fontWeight: "200",
      }}>
        お探しのページが見つかりませんでした
      </h1>

    </Layout>
  </div>

)

export default NotFound
